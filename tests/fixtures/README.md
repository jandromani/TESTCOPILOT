This folder should contain sample PDF files used for testing the PDF parser.

Place one or more `.pdf` files here and run `node src/runner.js tests/fixtures/<your-file>.pdf` to test the pipeline locally.

Note: PDF parsing requires `pdf-parse` installed. Run `npm install` before testing.
